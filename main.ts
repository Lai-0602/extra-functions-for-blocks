function advancedShowNumber(number: number, interval: number) {
    basic.showNumber(number, interval);
}

function advancedShowString(string: string, interval: number) {
    basic.showString(string, interval);
}

